apiVersion: v1
kind: Service

metadata:
  name: postgres-service

spec:

  type: {{ .Values.service.type }}

  selector:
    app: postgres

  ports:

    - name: postgres

      port: {{ .Values.service.port }}

      targetPort: 5432
