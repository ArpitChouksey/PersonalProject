apiVersion: v1
kind: Service
metadata:
  name: kafka-service

spec:
  type: {{ .Values.service.type }}

  selector:
    app: kafka

  ports:

    - name: broker
      port: {{ .Values.service.port }}
      targetPort: 9092

    - name: controller
      port: 9093
      targetPort: 9093
