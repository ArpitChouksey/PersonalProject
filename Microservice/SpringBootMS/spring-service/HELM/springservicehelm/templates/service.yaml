apiVersion: v1
kind: Service
metadata:
  name: spring-service

spec:
  type: {{ .Values.service.type }}

  selector:
    app: spring-service

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
