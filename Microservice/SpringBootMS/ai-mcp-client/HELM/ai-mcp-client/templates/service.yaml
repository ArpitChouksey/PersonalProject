apiVersion: v1
kind: Service

metadata:
  name: ai-mcp-client

spec:

  type: {{ .Values.service.type }}

  selector:
    app: ai-mcp-client

  ports:

    - name: http

      port: {{ .Values.service.port }}

      targetPort: {{ .Values.app.port }}
