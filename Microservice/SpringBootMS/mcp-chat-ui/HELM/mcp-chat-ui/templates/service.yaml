apiVersion: v1
kind: Service

metadata:
  name: mcp-chat-ui

spec:

  type: {{ .Values.service.type }}

  selector:
    app: mcp-chat-ui

  ports:

    - name: http

      port: {{ .Values.service.port }}

      targetPort: {{ .Values.app.port }}
