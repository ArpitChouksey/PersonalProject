apiVersion: v1
kind: Service
metadata:
  name: python-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "python-service.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000

