{
  "dashboard": {
    "id": null,
    "title": "SRE-AUTO-POSTGRES",
    "tags": ["sre","postgres"],
    "timezone": "browser",
    "schemaVersion": 41,
    "version": 0,
    "panels": [
      {
        "id": 1,
        "title": "Postgres CPU Usage",
        "type": "timeseries",
        "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
        "targets": [
          {
            "expr": "sum(rate(container_cpu_usage_seconds_total{pod=~\"postgres.*\"}[5m]))"
          }
        ]
      },
      {
        "id": 2,
        "title": "Postgres Memory Usage",
        "type": "timeseries",
        "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
        "targets": [
          {
            "expr": "sum(container_memory_usage_bytes{pod=~\"postgres.*\"})"
          }
        ]
      },
      {
        "id": 3,
        "title": "Postgres Restart Count",
        "type": "stat",
        "gridPos": { "h": 6, "w": 24, "x": 0, "y": 8 },
        "targets": [
          {
            "expr": "sum(kube_pod_container_status_restarts_total{pod=~\"postgres.*\"})"
          }
        ]
      }
    ]
  },
  "overwrite": true
}
